



<bash>
python-enter-s5373 () {
    escript bahbeddy_boop.erl
}
</bash>

for i in 0..5 ; do $(python-enter-s5373)

</bash>


system_copy() ->
    <python>
        for i in range(9999):
            print(<erlang>random:uniform(5)</erlang>)
    </python>.

<python>
    def site5757():
        for i in range(9999):
            print(yield([]))

    def run():
        while True:
            x = site5757.next()
</python>

<python>
    def erlang_execute(site, args):
        return post_request("http://localhost:57157/erlang_execute/site", args)

    for i in range(9999):
        print(erlang_execute(siteblah, []))
</python>


python_execute(From, Site, Args) ->
    register_site(Site, From),
    {blah, blah},

    receive
        {erlang_execute, PyContinuation, PyArgs} ->
            PyContinuation ! random:uniform(5),
            python_execute(From, Site, Args)
    end.



python_enter(Site, Args) ->
    python_execute(self(), Site, Args),

    receive



system_copy(Args) ->
    python_enter(site1, Args)



main(Args) ->
    system_copy().

</erlang>


